apiVersion: v1
kind: Service
metadata:
  name: redis-headless
  namespace: url-shortener
spec:
  clusterIP: None          # headless
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
