apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-ssd
provisioner: k8s.io/minikube-hostpath
reclaimPolicy: Retain        # keep PV even when PVC deleted
volumeBindingMode: WaitForFirstConsumer  # provision when pod scheduled
